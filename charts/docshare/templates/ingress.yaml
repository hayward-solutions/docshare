{{- if .Values.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "docshare.fullname" . }}
  labels:
    {{- include "docshare.labels" . | nindent 4 }}
  {{- with .Values.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.ingress.className }}
  ingressClassName: {{ .Values.ingress.className }}
  {{- end }}
  rules:
    {{- range .Values.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                {{- if eq .service "api" }}
                name: {{ include "docshare.fullname" $ }}-api
                port:
                  number: {{ $.Values.api.service.port }}
                {{- else if eq .service "web" }}
                name: {{ include "docshare.fullname" $ }}-web
                port:
                  number: {{ $.Values.web.service.port }}
                {{- else if eq .service "gotenberg" }}
                name: {{ include "docshare.fullname" $ }}-gotenberg
                port:
                  number: {{ $.Values.gotenberg.service.port }}
                {{- else }}
                name: {{ include "docshare.fullname" $ }}-web
                port:
                  number: {{ $.Values.web.service.port }}
                {{- end }}
          {{- end }}
    {{- end }}
  {{- with .Values.ingress.tls }}
  tls:
    {{- range . }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
{{- end }}
