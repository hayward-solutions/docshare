apiVersion: v1
kind: Service
metadata:
  name: {{ include "docshare.fullname" . }}-web
  labels:
    {{- include "docshare.labels" . | nindent 4 }}
    app.kubernetes.io/component: web
spec:
  type: {{ .Values.web.service.type }}
  ports:
    - port: {{ .Values.web.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "docshare.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: web
